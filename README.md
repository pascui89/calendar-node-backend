# Backend MERN - Calendar

## Dev

1. Install the dependencies `npm install` 
2. Clone and rename `.env.template` to `.env`
3. Change the variable acourding you configuration
4. Run the server with `npm start`

## Deployment
[DEMO](https://calendar-node-backend.up.railway.app/)